<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <title>Xem chi tiết sản phẩm</title>
    <meta name="description" content="Đại Việt chuyên cung cấp điện thoại chính hãng, giá tốt, bảo hành uy tín. Giao hàng toàn quốc.">
  <meta name="keywords" content="điện thoại, smartphone, mua điện thoại, điện thoại giá rẻ, điện thoại chính hãng">
    <link
      rel="icon"
      type="image/x-icon"
      href="https://i.pinimg.com/736x/61/0c/bd/610cbd83032444da98b38a47a1ee9fb1.jpg"
    />
    <link href="/admin/css/styles.css" rel="stylesheet" />
    <script
      src="https://use.fontawesome.com/releases/v6.3.0/js/all.js"
      crossorigin="anonymous"
    ></script>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-4Q6Gf2aSP4eDXB8Miphtr37CMZZQ5oXLH2yaXMJ2w8e2ZtHTl7GptT4jmndRuHDT"
      crossorigin="anonymous"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css"
    />
  </head>

  <body class="sb-nav-fixed">
    <%- include('../header.ejs'); -%>
    <div id="layoutSidenav">
      <%- include('../sidenav.ejs'); -%>
      <div id="layoutSidenav_content">
        <main>
          <div class="container-fluid px-4">
            <h1 class="mt-4 mb-2 text-dark">Chỉnh sửa sản phẩm</h1>
            <ol class="breadcrumb mb-4">
              <li class="breadcrumb-item"><a href="/admin">Bảng điều khiển</a></li>
              <li class="breadcrumb-item active">Sản phẩm</li>
              <li class="breadcrumb-item active">Chỉnh sửa sản phẩm</li>
            </ol>
            <hr />
      
            <div class="container d-flex justify-content-center mt-5">
              <div class="col-md-8 col-lg-10 mb-5">
                <div class="card shadow rounded-4">
                  <div class="card-body p-5">
                    <h3 class="card-title text-center mb-4 text-dark fw-bold">
                      Thông tin chi tiết sản phẩm
                    </h3>
      
                    <form action="/admin/update-product/<%= product.id %>" method="post" enctype="multipart/form-data">
                      <input type="hidden" name="id" value="<%= product.id %>" />
      
                      <!-- Tên sản phẩm -->
                      <div class="form-floating mb-4">
                        <input type="text" class="form-control" id="name" name="name" value="<%= product.name %>" required />
                        <label for="name">Tên sản phẩm</label>
                      </div>
      
                      <!-- Giá & Số lượng -->
                      <div class="row mb-4">
                        <div class="col-md-6">
                          <div class="form-floating">
                            <input type="number" class="form-control" id="price" name="price" value="<%= product.price %>" required />
                            <label for="price">Giá (VND)</label>
                          </div>
                        </div>
                        <div class="col-md-6">
                          <div class="form-floating">
                            <input type="number" class="form-control" id="quantity" name="quantity" value="<%= product.quantity %>" required />
                            <label for="quantity">Số lượng</label>
                          </div>
                        </div>
                      </div>
      
                      <!-- Mô tả ngắn -->
                      <div class="form-floating mb-4">
                        <input type="text" class="form-control" id="shortDesc" name="shortDesc" value="<%= product.shortDesc %>" required />
                        <label for="shortDesc">Mô tả ngắn</label>
                      </div>
      
                      <!-- Mô tả chi tiết -->
                      <div class="form-floating mb-4">
                        <textarea class="form-control" id="detailDesc" name="detailDesc" style="height: 100px" required><%= product.detailDesc %></textarea>
                        <label for="detailDesc">Mô tả chi tiết</label>
                      </div>
      
                      <!-- Hãng sản xuất -->
                      <div class="form-floating mb-4">
                        <select name="factory" class="form-select">
                          <% factoryOptions.forEach(factory => { %>
                            <option value="<%= factory.value %>" <%= factory.value === product.factory ? 'selected' : '' %>>
                              <%= factory.name %>
                            </option>
                          <% }) %>
                        </select>
                        <label for="factory">Hãng sản xuất</label>
                      </div>
      
                      <!-- Dòng sản phẩm -->
                      <div class="form-floating mb-4">
                        <select name="target" class="form-select">
                          <% targetOptions.forEach(target => { %>
                            <option value="<%= target.value %>" <%= target.value === product.target ? 'selected' : '' %>>
                              <%= target.name %>
                            </option>
                          <% }) %>
                        </select>
                        <label for="target">Dòng sản phẩm</label>
                      </div>
      
                      <!-- Thông tin thêm -->
                      <div class="row mb-4">
                        <div class="col-md-6">
                          <div class="form-floating mb-3">
                            <input type="text" class="form-control" name="ram" value="<%= product.ram %>" required />
                            <label>RAM</label>
                          </div>
                          <div class="form-floating mb-3">
                            <input type="text" class="form-control" name="storage" value="<%= product.storage %>" required />
                            <label>Bộ nhớ</label>
                          </div>
                          <div class="form-floating mb-3">
                            <input type="text" class="form-control" name="os" value="<%= product.os %>" required />
                            <label>Hệ điều hành</label>
                          </div>
                          <div class="form-floating mb-3">
                            <input type="text" class="form-control" name="status" value="<%= product.status %>" required />
                            <label>Trạng thái</label>
                          </div>
                        </div>
      
                        <div class="col-md-6">
                          <div class="form-floating mb-3">
                            <input type="text" class="form-control" name="screen" value="<%= product.screen %>" required />
                            <label>Màn hình</label>
                          </div>
                          <div class="form-floating mb-3">
                            <input type="text" class="form-control" name="battery" value="<%= product.battery %>" required />
                            <label>Pin</label>
                          </div>
                          <div class="form-floating mb-3">
                            <input type="text" class="form-control" name="camera" value="<%= product.camera %>" required />
                            <label>Camera</label>
                          </div>
                          <div class="form-floating mb-3">
                            <input type="number" step="0.1" class="form-control" name="rating" value="<%= product.rating %>" min="0" max="5" />
                            <label>Đánh giá (0 - 5)</label>
                          </div>
                        </div>
                      </div>
      
                      <!-- Hình ảnh hiện tại -->
                      <div class="mb-4">
                        <label class="form-label">Hình ảnh hiện tại:</label><br />
                        <img src="/images/product/<%= product.image %>" alt="Hình ảnh sản phẩm" class="img-thumbnail" style="max-width: 200px; height: 200px"  loading="lazy"/>
                      </div>
      
                      <!-- Upload ảnh mới -->
                      <div class="mb-4">
                        <label for="image" class="form-label fw-semibold">Thay đổi hình ảnh</label>
                        <input type="file" class="form-control" name="image" accept="image/*" />
                      </div>
      
                      <!-- Nút thao tác -->
                      <div class="d-flex justify-content-center gap-4">
                        <a href="/admin/product" class="btn btn-outline-secondary px-4">
                          <i class="bi bi-arrow-left me-1"></i> Quay lại
                        </a>
                        <button type="submit" class="btn btn-primary px-4">
                          <i class="bi bi-save me-1"></i> Lưu thay đổi
                        </button>
                      </div>
                    </form>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </main>
        <%- include("../footer.ejs"); -%>
      </div>
      
    </div>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"
      crossorigin="anonymous"
    ></script>
    <script src="/admin/js/scripts.js"></script>
    <script
      src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.8.0/Chart.min.js"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
